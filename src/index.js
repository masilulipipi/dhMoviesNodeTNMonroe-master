const homeContent = require('./home');
const moreVoteContent = require('./masVotadas');
const contactContent = require('./contacto');

module.exports = { homeContent, moreVoteContent, contactContent };
